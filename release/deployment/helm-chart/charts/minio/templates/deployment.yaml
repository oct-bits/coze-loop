apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "application.name" . }}
spec:
  replicas: {{ .Values.deployment.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "application.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "application.name" . }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.image.pullSecrets }}
      volumes:
        - name: data
          emptyDir: { }
        - name: scripts
          configMap:
            name: {{ include "configmap-scripts.name" . }}
      terminationGracePeriodSeconds: {{ .Values.deployment.terminationGracePeriodSeconds }}
      containers:
        - name: {{ include "application.name" . }}
          image: {{ include "image.fullname" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.targetPort }}
          volumeMounts:
            - name: data
              mountPath: {{ .Values.deployment.dataMountPath }}
            - name: scripts
              mountPath: {{ include "scripts.path" . }}
              readOnly: true
          env:
            - name: COZELOOP_MINIO_APP_NAME
              value: {{ include "application.name" . }}
            - name: COZELOOP_MINIO_PORT_POD
              value: "{{ .Values.service.targetPort }}"
            - name: COZELOOP_MINIO_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "secret.name" . }}
                  key: user
            - name: COZELOOP_MINIO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "secret.name" . }}
                  key: password
            - name: COZELOOP_MINIO_BUCKET
              value: {{ .Values.bucket.name }}
          command: [ "/bin/bash" ]
          args: [ "{{ include "entrypoint.path" . }}" ]
          livenessProbe:
            exec:
              command: [ "/bin/sh", "{{ include "healthcheck.path" . }}" ]
            initialDelaySeconds: {{ .Values.liveness.startSeconds }}
            periodSeconds: {{ .Values.liveness.intervalSeconds }}
            timeoutSeconds: {{ .Values.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.liveness.shutdownFailureTimes }}